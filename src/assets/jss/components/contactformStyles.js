const contactfromStyles = theme => {
  return {
    requestForm: {
      maxWidth: 1000,
      padding: 25,
      // backgroundColor: "rgba(240, 240, 240, 0.9)",
      color: theme.palette.secondary.main
    },
    white: {
      color: "white"
    },
    select: {
      borderBottom: "1px solid #fff"
    }
  };
};

export default contactfromStyles;
